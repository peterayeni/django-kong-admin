============
Installation
============

At the command line::

    $ easy_install django-kong-admin

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-kong-admin
    $ pip install django-kong-admin